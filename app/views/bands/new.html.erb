<h1>New Band Entry</h1>

<form action="<%= bands_url %>" method="post">
   <%= auth_token %>
   <label for="band_name">Band Name</label>
   <input type="text"
          name="band[name]"
          id="band_id"
          value= "<%= @band.name  %>">
    <input type="submit">
</form>
