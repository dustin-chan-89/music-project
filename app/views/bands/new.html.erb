<h1>New Artist Entry</h1>
<form action="<%= bands_url %>" method="post">
   <label for="band_name">Artist Name</label>
   <input type="text"
          name="band[name]"
          id="band_id"
          value= "<%= @band.name  %>">
    <input type="submit">
</form>
