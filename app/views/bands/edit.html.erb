<h1>Change Artist Name<%= @current_band %></h1>
<form action="<%= bands_url %>" method="patch">
   <label for="band_name">Artist Name</label>
   <input type="text"
          name="band[name]"
          id="band_id"
          value= "<%= @band.name  %>">
    <input type="submit">
</form>
